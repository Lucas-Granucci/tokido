import priority from "./presentation/priority";
import duration from "./presentation/duration";
import category from "./presentation/category";

export const presentationConfigs = {
  priority,
  duration,
  category,
};

export { priority, duration, category };
export default presentationConfigs;
